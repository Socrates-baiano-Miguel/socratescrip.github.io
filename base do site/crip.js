document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('MIGS');

    imgclick.addEventListener('click', () => {
        const  letra = 'H' 
        searchBar.value += letra;
    }
);
}
);
 document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('LUC');

    imgclick.addEventListener('click', () => {
        const  letra = 'X' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('JOA');

    imgclick.addEventListener('click', () => {
        const  letra = 'M' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('LUP');

    imgclick.addEventListener('click', () => {
        const  letra = 'S' 
        searchBar.value += letra;
    }
);
}
);
 document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('Dl');

    imgclick.addEventListener('click', () => {
        const  letra = 'Ç' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('GABDIV');

    imgclick.addEventListener('click', () => {
        const  letra = 'F' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('JOOJ');

    imgclick.addEventListener('click', () => {
        const  letra = 'A' 
        searchBar.value += letra;
    }
);
}
);
 document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('MACONHA');

    imgclick.addEventListener('click', () => {
        const  letra = 'K' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('ANAB');

    imgclick.addEventListener('click', () => {
        const  letra = 'Â' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('RAFAF');

    imgclick.addEventListener('click', () => {
        const  letra = 'Z' 
        searchBar.value += letra;
    }
);
}
);
 document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('THU');

    imgclick.addEventListener('click', () => {
        const  letra = 'Ã' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('CAMIL');

    imgclick.addEventListener('click', () => {
        const  letra = 'L' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('JOSSS');

    imgclick.addEventListener('click', () => {
        const  letra = 'J' 
        searchBar.value += letra;
    }
);
}
);
 document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('LIN');

    imgclick.addEventListener('click', () => {
        const  letra = '?' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('MANDR');

    imgclick.addEventListener('click', () => {
        const  letra = 'C' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('TAUO');

    imgclick.addEventListener('click', () => {
        const  letra = 'Õ' 
        searchBar.value += letra;
    }
);
}
);
 document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('GABS');

    imgclick.addEventListener('click', () => {
        const  letra = 'B' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('MRANG');

    imgclick.addEventListener('click', () => {
        const  letra = 'T' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('ALIN');

    imgclick.addEventListener('click', () => {
        const  letra = 'Á' 
        searchBar.value += letra;
    }
);
}
);
 document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('LAN');

    imgclick.addEventListener('click', () => {
        const  letra = '#' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('JOANN');

    imgclick.addEventListener('click', () => {
        const  letra = 'D' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('LINLIN');

    imgclick.addEventListener('click', () => {
        const  letra = 'G' 
        searchBar.value += letra;
    }
);
}
);
 document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('lt');

    imgclick.addEventListener('click', () => {
        const  letra = 'Q' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('JOAG');

    imgclick.addEventListener('click', () => {
        const  letra = 'W' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('AMIMIR');

    imgclick.addEventListener('click', () => {
        const  letra = 'Ô' 
        searchBar.value += letra;
    }
);
}
);
 document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('LULU');

    imgclick.addEventListener('click', () => {
        const  letra = 'É' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('LULUM');

    imgclick.addEventListener('click', () => {
        const  letra = 'I' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('LÇÇ');

    imgclick.addEventListener('click', () => {
        const  letra = 'Û' 
        searchBar.value += letra;
    }
);
}
);
 document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('LEL');

    imgclick.addEventListener('click', () => {
        const  letra = 'N' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('TR');

    imgclick.addEventListener('click', () => {
        const  letra = 'Ê' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('RT');

    imgclick.addEventListener('click', () => {
        const  letra = 'Í' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('RTE');

    imgclick.addEventListener('click', () => {
        const  letra = '!' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('HUM');

    imgclick.addEventListener('click', () => {
        const  letra = 'Ó' 
        searchBar.value += letra;
    }
);
}
);
 document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('RF');

    imgclick.addEventListener('click', () => {
        const  letra = '%' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('EU');

    imgclick.addEventListener('click', () => {
        const  letra = 'E' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('CALV');

    imgclick.addEventListener('click', () => {
        const  letra = 'O' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('AA');

    imgclick.addEventListener('click', () => {
        const  letra = 'Í' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('XXX');

    imgclick.addEventListener('click', () => {
        const  letra = 'P' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('DRU');

    imgclick.addEventListener('click', () => {
        const  letra = 'U' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('HT');

    imgclick.addEventListener('click', () => {
        const  letra = 'Ú' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('KAPA');

    imgclick.addEventListener('click', () => {
        const  letra = 'V' 
        searchBar.value += letra;
    }
);
 }
);
document.addEventListener('DOMContentLoaded', () => {
    const bar = document.getElementById('searchBar');
    const imgclick = document.getElementById('DTWO');

    imgclick.addEventListener('click', () => {
        const  letra = 'Y' 
        searchBar.value += letra;
    }
);
 }
);



